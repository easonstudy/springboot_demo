<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gmh.cjcx.dao.MemberDao">

	<select id="findById" resultType="MemberDto">
		SELECT * FROM T_MEMBER WHERE ID=#{id}
	</select>

</mapper>